import { Route, Switch } from "react-router-dom";

const Routes = () => {
  <Switch>
    <Route path={LandingBasePath} exact />
  </Switch>;
};

export const LandingBasePath = "";
export default Routes;
